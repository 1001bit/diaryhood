// main
const mainElem = document.getElementsByTagName("main")[0] as HTMLDivElement;

// avatar
const changeAvatarElem = document.getElementById(
	"change-avatar"
) as HTMLDivElement;
const avatarElem = document.getElementById("avatar") as HTMLImageElement;

// name
const nameElem = document.getElementById("name") as HTMLDivElement;
const changeNameElem = document.getElementById("change-name") as HTMLDivElement;
const nameInputElem = document.getElementById(
	"name-input"
) as HTMLTextAreaElement;
// user steps
const userStepsElem = document.getElementById("user-steps") as HTMLDivElement;

// paths
const pathsElem = document.getElementById("paths") as HTMLDivElement;
// createPath
const pathCreateBoxElem = document.getElementById(
	"path-create-box"
) as HTMLDivElement;
const createPathButton = document.getElementById(
	"create-path"
) as HTMLDivElement;
const pathNameInputElem = document.getElementById(
	"path-name-input"
) as HTMLTextAreaElement;
// samplePath
const samplePathElem = document.getElementById("sample-path") as HTMLDivElement;
// no paths
const noPathsElem = document.getElementById("no-paths") as HTMLDivElement;
