syntax = "proto3";

package userpb;

option go_package = "./authpb";

service AuthService {
    rpc Authenticate(AuthRequest) returns (AuthResponse) {}
}

message AuthRequest {
    string login = 1;
}

message AuthResponse {
    string accessJWT = 1;
    string refreshJWT = 2;
    string username = 3;
}